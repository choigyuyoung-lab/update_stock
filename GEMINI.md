# 🤖 [K-올라운드 마스터] 프로젝트 AI 개발 가이드 및 시스템 규칙 (GEMINI.md)

이 문서는 본 프로젝트(`update_stock`)의 아키텍처, 퀀트 분석 엔진, 노션 DB 연동 원칙 및 코딩 규칙을 정의합니다.
모든 AI 어시스턴트는 이 프로젝트의 코드를 추가, 수정, 리팩토링할 때 본 지침을 반드시 준수해야 합니다.

---

## 🚨 1. 절대 준수 원칙 (Critical Constraints)

1. **자동 Git 커밋/푸시 금지**:
   - **어떠한 경우에도 `git commit` 또는 `git push` 명령어를 자동으로 실행하지 마십시오.**
   - 코드 수정 및 테스트까지만 완료하고, 최종 커밋/푸시는 사용자가 직접 수행하도록 안내해야 합니다.
2. **터미널 명령어 사전 한국어 안내 원칙**:
   - 로컬 터미널 명령어 실행이 필요할 경우, **무엇을 위해 어떤 명령어를 실행하는지 반드시 한국어로 명확히 사전 설명**한 후 실행해야 합니다.
   - 사용자 승인 팝업이 발생할 것을 대비해 목적과 명령어 내용을 한국어로 명시합니다.
3. **노션 DB 스키마 방어 로직 (Graceful Property Check)**:
   - 노션 속성을 업데이트할 때는 반드시 노션 DB에 해당 열이 존재하는지 사전 검사(`if field in props`)해야 합니다.
   - 사용자가 노션에서 특정 열을 삭제하거나 이름을 변경하더라도 스크립트가 크래시되지 않고 안전하게 통과해야 합니다.
4. **독립된 워크플로 구조 유지**:
   - 시세 수집(`*_price_update.yml`), 재무 수집(`*_finance_update.yml`), 주간 리포트(`generate_report.yml`)는 완전히 분리되어 독립적으로 실행되어야 합니다.

---

## 🧠 2. 5대 퀀트 엔진 및 핵심 수식 규칙

모든 퀀트 지표 산출 로직은 다음의 표준 수식과 명명 규칙을 엄격히 따라야 합니다:

| 퀀트 지표 | 노션 열 이름 | 데이터 형식 | 산출 공식 및 판정 기준 |
| :--- | :--- | :---: | :--- |
| **200일선** | `200일선` | Number | 최근 200영업일 종가 이동평균 (`Close.rolling(200).mean()`) |
| **수급선** | `수급선` | Number | • 🇺🇸 미국: **50일선** (`Close.rolling(50).mean()`)<br>• 🇰🇷 한국: **60일선** (`Close.rolling(60).mean()`) |
| **추세** | `추세` | Select / Text | • 🇺🇸 미국: `▲ 기관주도` (현재가 $\ge$ 50MA $\ge$ 200MA) / `━ 눌림조정` / `▼ 하락추세`<br>• 🇰🇷 한국: `▲ 수급유입` (현재가 $\ge$ 60MA $\ge$ 200MA) / `━ 박스권세` / `▼ 하락추세` |
| **스마트 가이드** | `스마트 가이드` | Select / Text | `▲ 분할매수` / `▲ 추세탑승` / `▲ 상승유지` / `━ 눌림지지` / `▼ 바닥확인` / `▼ 하락관망` |
| **12M 모멘텀** | `12M 모멘텀` | Number (%) | $\frac{\text{현재가} - \text{1년전(252일전)가격}}{\text{1년전(252일전)가격}}$ (노션 백분율 소수점) |
| **모멘텀 진단** | `모멘텀 진단` | Select / Text | `▲ 주도대장` (+50%↑) / `▲ 실적지속` (+20~+50%) / `▲ 시장동행` (+5~+20%) / `━ 방향탐색` (-10~+5%) / `▼ 자금이탈` (-10%↓) |
| **52주 낙폭** | `52주 낙폭` (또는 `낙폭율`) | Number (%) | $\frac{\text{현재가} - \text{52주최고가}}{\text{52주최고가}}$ (고점 대비 하락률 음수값) |
| **60일 변동성** | `60일 변동성` | Number (%) | $\text{std}(\text{일일수익률}_{60}) \times \sqrt{252}$ (연환산 변동성) |
| **위험도 등급** | `위험도 등급` | Select / Text | `▲ 비중확대` (<20%) / `━ 정상비중` (20~35%) / `▼ 비중조절` (35~60%) / `▼ 소액접근` (>60%) |
| **3M 동적 밴드** | - | Macro Data | 환율 및 10년물 금리의 최근 60영업일 백분위수($Q_{25}, Q_{50}, Q_{75}$) 기반 국면 판정 |
| **포트폴리오 VaR** | - | Report Metric | $\text{총평가자산} \times 1.65 \times \frac{\sum (w_i \times \sigma_i)}{\sqrt{52}}$ (95% 1-Week VaR) |
| **스마트 밸류 에버리징** | - | Report Table | $\text{Score}_i = (\text{목표비중}_i + \max(0, -\text{괴리율}_i \times 2)) \times \text{추세가중치} \times \text{낙폭가중치}$ |

---

## 🛠️ 3. 데이터 소스 및 API 사용 원칙

1. **국내 종목 & 7대 자산 ETF**:
   - 1년치 시계열 데이터 조회 시 `FinanceDataReader (fdr)`를 최우선 사용합니다. (초고속 및 안정적 250일치 제공)
   - KIS API는 기본 시세/투자지표(`FHKST01010100`) 위주로 호출하며, 불필요한 투자의견 API(`HHDFS76700100`) 등은 호출하지 않습니다.
2. **해외 주식/ETF**:
   - `yfinance`의 `stock.history(period="1y")`로 200일선, 모멘텀, 변동성을 일괄 계산합니다.
3. **노션 수식 대체 원칙**:
   - `52주 위치`, `안전마진`, `전일대비` 등 단순 사칙연산 지표는 노션 수식 열에 의존하지 않고 **파이썬 코드 내부에서 100% 자체 계산**되도록 구현합니다.

---

## 📋 4. 프로젝트 핵심 파일 맵

```text
d:\Github IDE\update_stock/
├── macro_service.py              # 실시간 글로벌 매크로 지표, 3M 동적 밴드, 7대 자산 퀀트 순위표 엔진
├── generate_portfolio_report.py  # 4대 노션 DB 통합 데이터 수집, VaR 산출, 스마트 밸류 에버리징 리포트 생성기
├── config_portfolio.py           # 7대 자산군 목표 비중(%), AI 시스템 프롬프트 및 리포트 마크다운 템플릿
├── ai_service.py                 # Google Gemini API + Google Search Grounding 4단계 폴백 AI 리포트 생성기
├── update_finance_kr.py          # 국내 주식 재무/퀀트 지표 수집 및 노션 DB 적재
├── update_finance_us.py          # 해외 주식 재무/퀀트 지표 수집 및 노션 DB 적재
├── update_price_kr.py            # 국내 주식 실시간 현재가 업데이트
├── update_price_us.py            # 해외 주식 실시간 현재가 업데이트
├── notion_utils.py               # 노션 API 페이지네이션, 멀티스레드 안전 업데이트, 토큰 캐시 유틸리티
├── sync_manager.py               # 회사/집 PC 자동 인식 및 스마트 Git 동기화 매니저
├── 작업시작_동기화.bat           # 작업 시작 시 원클릭 최신 동기화 실행 배치 파일
├── 작업종료_동기화.bat           # 작업 종료/퇴근 시 원클릭 Git Push 자동화 배치 파일
└── QUANT_INTEGRATION_HANDOFF.md  # 퀀트 시스템 통합 및 인수인계 문서
```

---

## ✍️ 5. 리포트 문체 및 작성 스타일 규칙
- 진단 리포트의 모든 분석 문장은 **명사형 종결어미 (`~함`, `~임`, `~필요`, `~권고`)**로 작성해야 합니다.
- HTML 태그를 사용하지 말고 표준 GitHub Flavored Markdown (표, 굵은 글씨, 불릿)으로 작성합니다.

---

## 🔄 6. 재택/이동 근무 환경 동기화 규칙 (Work-from-Home Protocol)

사용자가 "집에서 작업할 수 있도록 취해야 할 조치를 알려줘" 또는 PC 간 작업 이동에 대해 문의할 경우, **원클릭 배치 파일 실행** 또는 다음의 3단계 표준 프로토콜을 안내해야 합니다:

1. **[현재 PC] 이동/퇴근 전 원클릭 조치**:
   - `작업종료_동기화.bat` 더블클릭 (자동으로 `git add`, 위치/시간 태그 커밋, `git push` 완료)
   - `.env` 파일 점검 (보안상 Git에 포함되지 않으므로, 집 PC에 최신 API 키가 설정되어 있는지 확인/백업 안내)
2. **[집 PC] 작업 시작 시 원클릭 조치**:
   - `작업시작_동기화.bat` 더블클릭 (자동으로 환경 인식, `git pull`, `.env` 존재 여부 점검)
   - 필요 시 패키지 동기화 (`pip install -r requirements.txt`)
3. **[집 PC] 작업 완료 후 원클릭 조치**:
   - 집에서 작업 완료 후 `작업종료_동기화.bat` 더블클릭으로 원격 저장소 업로드

